/*
 * AdvisorPage Messages
 *
 * This contains all the text for the AdvisorPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AdvisorPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AdvisorPage container!',
  },
});
